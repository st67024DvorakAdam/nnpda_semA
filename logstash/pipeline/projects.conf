input {
  jdbc {
    jdbc_driver_library => "/usr/share/logstash/drivers/postgresql-42.7.3.jar"
    jdbc_driver_class => "org.postgresql.Driver"
    jdbc_connection_string => "jdbc:postgresql://postgres:5432/semA"
    jdbc_user => "user"
    jdbc_password => "heslo"
    schedule => "* * * * *"
    statement => "SELECT id, name, description, status, owner_id FROM projects;"
  }
}

filter {
  # Převod status nebo jiné transformace pokud bude potřeba
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "projects"
    document_id => "%{id}"
  }
  stdout { codec => json_lines }
}
