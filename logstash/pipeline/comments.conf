input {
  jdbc {
    jdbc_driver_library => "/usr/share/logstash/drivers/postgresql-42.7.3.jar"
    jdbc_driver_class => "org.postgresql.Driver"
    jdbc_connection_string => "jdbc:postgresql://postgres:5432/semA"
    jdbc_user => "user"
    jdbc_password => "heslo"
    schedule => "* * * * *"
    statement => "SELECT id, created_at, text, author_id, project_id, ticket_id FROM comments;"
  }
}

filter {
  date {
    match => ["created_at", "ISO8601"]
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "comments"
    document_id => "%{id}"
  }
  stdout { codec => json_lines }
}
